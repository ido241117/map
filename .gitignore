# Dependencies
node_modules/

# Build outputs
dist/
*.tsbuildinfo
.vite/

# Environment & secrets
.env
.env.local
.env.*.local
!.env.example

# Logs
*.log
server.out.log
server.err.log

# OS
.DS_Store
Thumbs.db
Desktop.ini

# IDE
.idea/
.vscode/
*.swp
*.swo

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/

# Local data (imports, dumps, excel — not for repo)
data/
db-export/*
!db-export/RESTORE.md
# OSM slim dump nhỏ (~17 MB) — track để VPS git pull là restore được
!db-export/osm_highways.dump

# Local-only tooling (on disk for dev/ops, not pushed to remote)
/app.js
/index.html
/styles.css
/lib/
/ecosystem.config.cjs
/scripts/**
!scripts/kill-port.ps1
!scripts/with-uv-threadpool.js
# Docker / DB ops scripts — tracked so VPS can pull them
!scripts/docker-*.js
!scripts/docker-*.sql
!scripts/wait-elasticsearch.js
!scripts/extract-osm-highways.js
!docker/
*.zip
*.dump
!db-export/osm_highways.dump
*.pbf

# Crawler / scraper tooling (local only)
scan/

# Accidental spreadsheet drops
*.xlsx
plan.md
run.md
db.md
